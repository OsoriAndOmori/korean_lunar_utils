# korean_lunar_utils

Lunar/Solar conversion utility with a structured converter API.

Usage
-----

Import the package and use `LunarSolarConverter` to convert between lunar and solar dates.

Publishing
----------
This package is prepared for publishing to pub.dev. Run:

```bash
dart pub publish --dry-run
```
